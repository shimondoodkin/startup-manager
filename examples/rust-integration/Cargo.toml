[package]
name = "startup-manager-jobs-board"
version = "0.1.0"
edition = "2021"
description = "Example: a Rust app rendering startup-manager's job roster from the sm-export JSON snapshot"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
